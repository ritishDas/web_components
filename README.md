fullpageslider : 

Usage steps 

*Copy the slider.jsx file to your component folder and copy the css from css file to your project's css file. 
*Import the slider function and use it by passing an array of images (paths or links ).
